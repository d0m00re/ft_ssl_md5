-p : echo STDIN to STDOUT
-q : quit mode
-r : reverse the format of output
-s : print the sum of a given string

# recuperer l entree standard
echo "pickle rick" | md5
c5e433c1dbd7ba01e3763a9483e74b04

# -q ne pas aff
echo "Pity the living." | ./ft_ssl md5 -q -r


-p : le p peut etre un peu tricky : dans le cas o

Dans le cas ou
	./hash md5 :on prend l entree standart

Dans le cas ou
	./hash md5 file  # on prend pas  l entree standart

Dans le cas ou
	./hash md5 file -q # on prend le fichier ainsi que l entree standart

let s go bitch

trois type d affichage posisble:
	-q : get fd0
	-s: display stirng
	file : display hash of the file

